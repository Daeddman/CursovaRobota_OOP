<Window x:Class="CursovaRobota.Views.EventDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подія" Height="260" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF2196F3"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="OnSurfaceBrush" Color="#FF333333"/>
        <SolidColorBrush x:Key="OnSurfaceVariantBrush" Color="#99000000"/>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="{StaticResource SurfaceBrush}" Padding="16" Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding Title}"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"/>

            <TextBlock Grid.Row="1"
                       Text="{Binding Date, StringFormat='{}{0:dd.MM.yyyy}'}"
                       FontSize="14"
                       Foreground="{StaticResource OnSurfaceVariantBrush}"
                       Margin="0,8,0,8"/>

            <TextBlock Grid.Row="2"
                       Text="{Binding Description}"
                       TextWrapping="Wrap"
                       FontSize="13"
                       Foreground="{StaticResource OnSurfaceBrush}"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Видалити"
                        Style="{StaticResource DialogButtonStyle}"
                        Background="#FFF44336"
                        Click="Delete_Click"/>
                <Button Content="Закрити"
                        Style="{StaticResource DialogButtonStyle}"
                        Margin="8,0,0,0"
                        Click="Close_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>